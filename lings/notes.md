# Structure
The structure of rustlings repo:
- `lings/` ← Kernel directory
	- `<xx-ling>/` ← Relearn of single theme (e.g. 01-introduction)
		- `main.rs` ← Example code (\*)
		- `notes.md` ← Notes, TL;DRs, references
	- `notes.md` ← Structure (you're here)

\* - stands for optional
